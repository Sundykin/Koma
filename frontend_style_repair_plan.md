# Koma 项目前端样式深度审核与修复方案

在对分镜页面（Storyboard）及其关联的右侧工具面板（资产、剧本、推理、风格、导出、工作流预设）进行深度审核后，我们发现当前系统在 UX（用户体验人性化）和 UI（视觉与交互一致性规范）上存在大量不足。

以下是所有“反人类（不人性化）”及“不跟随规范（不一致）”的设计清单及修复方案：

---

## 一、 “反人类”（不人性化 UX）的设计痛点

### 1. 资产面板 (AssetManagerPanel)
*   **空状态缺乏引导（死胡同）**：当资产列表为空时，只显示了一个干瘪的 `Empty description="暂无数据"`，用户在空状态下没有直接的“新建”按钮或行动号召（CTA），必须去顶部找全局的新建按钮。
*   **卡片点击区域与按钮冲突**：资产卡片整张可点击（绑定了编辑事件），但内部又挤入了 `生成` 和 `编辑` 按钮，由于使用了过小的间距（`Space size={4}`），极其容易引发误触。
*   **平庸的交互反馈**：卡片的 Hover 态只改变了边框颜色（`hover:border-zinc-500`），没有背景色或阴影的反馈，在深色模式下视觉反馈非常微弱，让人感觉“点不下去”。

### 2. 剧本面板 (ScriptStudioPanel)
*   **强硬的高度限制**：剧本输入框（TextArea）写死了 `rows={12}` 和 `rows={10}`，在小屏幕上占据过大空间，而在大屏幕上又无法充分利用空间，且缺乏平滑的自适应高度展开机制。
*   **编辑状态突兀跳跃**：在确认写入阶段，点击列表项进入编辑状态时，直接原地渲染一个 TextArea，导致原本的行高瞬间撑大，列表发生严重的视觉跳跃，极其打断用户心流。

### 3. 推理面板 (ChapterInferencePanel)
*   **多重嵌套的“俄罗斯套娃”（Box-in-box）**：草稿列表为了表示选中状态，给整行加了 `!bg-emerald-950/20`，而行内的数据块又裹了一层 `bg-zinc-950/70` 和边框。多重高对比度的方框嵌套在一起，视觉极其压抑厚重。
*   **无根的进度条**：推理和改写进度条（Progress）直接孤零零地挂在按钮下方，缺乏容器包裹和动画过渡，显得像个临时调试组件。

### 4. 导出与风格面板 (Export & Style)
*   **排版强依赖字数（脆弱的表单）**：导出配置中的标签（如“图片格式”、“超分辨率”）被粗暴地设定为 `w-20`（固定宽度）。这种强行写死宽度的做法在未来添加更长文本或国际化时必定会引发错位，完全不符合现代响应式表单规范。
*   **喧宾夺主的状态标签**：大量使用了带高亮底色和边框的 Tag（如风格面板的“Pending Plan”），有些甚至还加上了 `uppercase tracking-[0.14em]` 这种极具攻击性的字距设计，对用户视觉造成了不必要的压迫。

---

## 二、 “不跟随规范”（UI不一致）的设计痛点

### 1. 选中/激活主题色精神分裂
*   **风格面板**：使用翠绿色 `border-emerald-600` 表示选中。
*   **导出面板**：使用蓝色 `border-blue-600` 表示选中。
*   **工作流预设面板**：使用天蓝色 `border-sky-700 bg-sky-950/10` 表示选中。
*   **结论**：整个侧边栏各自为战，没有统一的 Primary Color，给用户一种拼凑的廉价感。

### 2. 容器内外边距（Padding/Margin）各自为政
*   **跳跃的边缘**：抽屉容器 `ToolPanelDrawer` 没有统一处理内容区的内边距，而是下放给每个面板。
    *   `AssetManagerPanel` 和 `ExportCenterPanel` 顶部带分割线，内边距为 `p-4`。
    *   `StyleSettingsPanel` 搜索框用了 `px-4 pt-3 pb-2`。
    *   `WorkflowRecipesPanel` 用了 `px-4 py-4` 和内部的卡片间距。
*   **结论**：当用户在不同面板间切换时，内容区的左侧或顶部边缘会出现明显的几像素抖动，严重破坏了产品的高级感。

### 3. 滥用强制覆盖（CSS Hacks）
*   在 `ScriptStudioPanel` 中出现了 `[&_.ant-collapse-panel]:!bg-zinc-900` 这样的代码。这不仅绕过了 Ant Design 的官方主题定制（ConfigProvider）机制，而且滥用 `!important` 使得后续的样式覆盖和主题切换变得极其困难。

---

## 三、 全面修复行动指南 (Action Plan)

为了彻底根除上述问题，需执行以下系统性改造：

### 1. 统一核心视觉语言 (Design System Sync)
*   **确立全局 Primary 主题色**：统一将所有可交互卡片的“选中/激活（Active）”状态修改为项目标准的主题色（推荐统一使用 Ant Design 默认的 Blue `blue-500` 或 Tailwind 的 `sky-500`），全面清退 `emerald-600`、`blue-600`、`sky-700` 混用的乱象。
*   **统一 Card 交互态**：所有列表项和卡片的 Hover 必须包含轻微的背景提亮（如 `hover:bg-zinc-800/80`），而不仅是边框变色，提供更人性化的点击暗示。

### 2. 重构表单与排版 (Layout Refactor)
*   **消灭硬编码表单**：将 `ExportCenterPanel` 中的 `w-20` 标签全部重构，使用 Ant Design 原生的 `<Form layout="horizontal">` 或标准的 CSS Grid（`grid-cols-[auto_1fr]`）来实现自适应对齐。
*   **收口容器 Padding**：在 `ToolPanelDrawer` 层面统一设置 `padding: 16px` 的包裹容器，并强制移除所有子面板顶层的 `p-4` 或 `px-4`，保证切换面板时内容区域纹丝不动。

### 3. 改善复杂数据展示体验 (UX Improvements)
*   **降噪处理“俄罗斯套娃”**：在 `ChapterInferencePanel` 中，废除外层的整行高亮背景，改用左侧边框高亮（如 `border-l-2 border-blue-500`）来表示选中，内部数据块使用隐形的分割线代替厚重的卡片包裹。
*   **人性化空状态**：在 `AssetManagerPanel` 等地方的 Empty 状态下，直接注入一个突出的“立即创建”大按钮，形成业务闭环。
*   **平滑的编辑体验**：在 `ScriptStudioPanel` 的列表编辑中，引入平滑的高度过渡，并确保编辑状态的 TextArea 高度能根据内容自适应展开，而不是生硬的定高。
*   **清理 CSS Hack**：全面清除组件中 `!important` 相关的暴力覆盖，统一使用 Ant Design v5 的 `styles` 和 `classNames` 属性，或使用标准的 Tailwind 深色类进行优雅的定制。
