Gemini-3-Pro-Image-preview（Nano banana2）
Gemini-3-Pro 图像生成
使用 Google Gemini 3 Pro 模型生成图像，更强大的图像生成能力

POST
https://toapis.com/v1/images/generations

试一试
Google Gemini 3 Pro 强大图像生成模型 (Nano banana2)
通过 model 参数选择 gemini-3-pro-image-preview 模型
支持文本到图像生成
异步任务管理，通过任务 ID 查询结果
​
Authorizations
​
Authorization
string必填
所有接口均需要使用 Bearer Token 进行认证
获取 API Key：访问 API Key 管理页面 获取您的 API Key
使用时在请求头中添加：
Authorization: Bearer YOUR_API_KEY
​
Body
​
model
string默认值:"gemini-3-pro-image-preview"必填
图像生成模型名称
可用模型别名：nano-banana-pro
示例："gemini-3-pro-image-preview"
​
prompt
string必填
图像生成的文本描述
最长 1000 个字符
​
size
string
图像生成的尺寸比例
支持的格式：
比例：1:1, 2:3, 3:2, 3:4, 4:3, 4:5, 5:4, 9:16, 16:9, 21:9
​
n
integer默认值:1
生成图像的数量
固定为 1
⚠️ 注意： 必须是纯数字（如 1），不要加引号，否则会报错
​
metadata
object
元数据参数，用于传递额外的配置选项
显示 支持的元数据字段

​
image_urls
object[]
参考图像 URL 列表，用于图生图或图像编辑
💡 快速填充（试一试功能）：
点击 ”+ Add an item” 添加图片 URL
输入完整的图片 URL 地址或 base64 数据
显示 详细字段说明

限制： 最多 14 张图片
​
Response
​
id
string
任务唯一标识符，用于查询任务状态
​
object
string
对象类型，固定为 generation.task
​
model
string
使用的模型名称
​
status
string
任务状态
queued - 排队等待处理
in_progress - 处理中
completed - 成功完成
failed - 失败
​
progress
integer
任务进度百分比（0-100）
​
created_at
integer
任务创建时间戳（Unix 时间戳）
​
metadata
object
任务元数据




获取图片任务状态
查询图片生成任务的状态和结果

GET
https://toapis.com/v1/images/generations/{task_id}

试一试
查询异步图片生成任务的执行状态和结果
实时状态更新和进度跟踪
任务完成时获取生成的图片
支持多语言返回（zh/en/ko/ja）
所有图片生成任务都是异步执行的。提交任务后，您需要通过查询接口获取任务状态和结果。
​
Authorizations
​
Authorization
string必填
所有接口均需要使用 Bearer Token 进行认证
获取 API Key：
访问 API Key 管理页面 获取您的 API Key
使用时在请求头中添加：
Authorization: Bearer YOUR_API_KEY
​
Path Parameters
​
task_id
string必填
图片生成 API 返回的任务 ID
​
Response
​
id
string
任务唯一标识符
​
object
string
对象类型，固定为 generation.task
​
model
string
使用的图片生成模型
​
status
string
任务状态
queued - 排队等待处理
in_progress - 处理中
completed - 成功完成
failed - 失败
​
progress
integer
任务进度百分比（0-100）
​
created_at
integer
任务创建时间（Unix 时间戳）
​
completed_at
integer
任务完成时间（Unix 时间戳，仅完成时返回）
​
expires_at
integer
图片 URL 过期时间（Unix 时间戳，仅完成时返回）
​
result
object
任务结果（仅成功时返回）
显示 属性

​
error
object
错误信息（仅失败时返回）
显示 属性

​
任务状态说明
状态	说明	是否终态	建议操作
queued	任务排队等待处理	❌	等待 2-3 秒后重试查询
in_progress	任务正在处理中	❌	等待 3-5 秒后重试查询
completed	任务成功完成	✅	从 result.data[0].url 获取图片
failed	任务处理失败	✅	检查 error 信息
​
轮询策略建议
初始等待: 2 秒
轮询间隔: 3 秒
最大等待: 120 秒
典型耗时: 5-30 秒