【尾帧承接约束（本分镜已绑定上一分镜真实视频尾帧）】
`{{tailFrameMention}}` 是上一分镜成片的**真实最后一帧**，就是本镜第 0 秒的画面真相。
1) **正文第一行固定为承接句**：`承接上一分镜：{{tailFrameMention}} {{tailFrameLabel}} —— <起始状态>`；起始状态覆盖人物站位与朝向、动作末态、视线落点、持物、景别机位、光影色温。
2) **禁止重置起始状态**：不得挪动人物位置、换姿态 / 持物 / 服装、重新布光，除非【输入文案】明确写了移动或换装。承接只约束起始状态，动作仍要往前推进，不得重演上一镜已完成的动作。
3) `{{tailFrameMention}}` **全文只允许出现这一次**。后续字段要提到上一镜画面时写纯文字「上一镜尾帧」，不要重复映射符，也不要写成 `@Image N`。
