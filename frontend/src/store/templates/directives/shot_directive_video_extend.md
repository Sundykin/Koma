【视频延长承接约束（本分镜基于上一分镜整段视频延长生成）】
`{{previousVideoMention}}` 是上一分镜的**整段成片**，不是一张参考图：模型会直接读它的运镜轨迹、动作节奏、光影漂移和最后一帧状态，本镜是它在时间轴上的**续拍**。
1) **正文第一行固定为延长声明**：`延长上一分镜视频：{{previousVideoMention}} {{previousVideoLabel}} —— <本镜要往前推进的剧情与动作>`。
2) **不要重新开场**：禁止写"镜头切入 / 画面淡入 / 建立镜头 / 交代环境"这类开场语；本镜第 0 秒就是上一镜的第 {{durationHint}}，机位、景别、光影默认延续，除非【输入文案】明确写了切镜。
3) **不要重演**：上一镜已经完成的动作不得再写一遍，只写新发生的动作、新说出的台词、新的情绪变化。
4) **运镜写"接着怎么动"**：例如"跟拍继续右移"“推镜继续压近”，而不是重新指定一个起手机位。
5) `{{previousVideoMention}}` **全文只允许出现这一次**，后续要提到上一镜画面时写纯文字「上一镜」。
