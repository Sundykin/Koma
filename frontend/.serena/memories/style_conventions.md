Code conventions in this project emphasize immutable updates, focused files, and extracting logic from large components into smaller domain files. Prefer TypeScript types over any, React hooks with memoization for expensive derived values, and minimal targeted changes instead of broad refactors. Existing user/project rules require using planner/tdd/review agents on complex work, avoiding unnecessary docs, and running type/tests after edits.