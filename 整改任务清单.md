# Koma 整改任务清单

## 1. 说明

本清单基于 [分析报告.md](/Users/sunmeng/workspace/Koma/分析报告.md) 拆解，目标是把结论转成可排期、可验收、可追踪的执行项。

任务设计原则：

- 优先处理会扩大系统风险的问题
- 每个任务尽量落到单一负责人视角
- 每项任务都附验收标准，避免“做了但无法判断是否完成”

---

## 2. P0 任务

### P0-1 密钥安全治理

- 负责人角色：架构师 + 安全审计 + 后端开发 B
- 预计工期：2-3 天
- 目标：
  - 解决 API Key 明文存储问题
  - 将长期密钥从渲染进程移出
- 主要改动方向：
  - 接入系统安全存储（Keychain / Credential Manager / libsecret）
  - 普通设置与密钥设置分离
  - 渲染层不再直接持久化明文密钥
- 依赖：
  - 无
- 验收标准：
  - 本地设置文件和 localStorage 中不再出现明文 API Key
  - 渲染进程无法直接读取长期密钥原文
  - 旧配置迁移后仍可正常使用已有模型配置

### P0-2 收紧渲染层能力暴露

- 负责人角色：架构师 + 安全审计 + 后端开发 B
- 预计工期：3-5 天
- 目标：
  - 缩小 renderer -> main 的攻击面
  - 收紧 preload、FS、网络和本地协议能力
- 主要改动方向：
  - 禁止 `controller/*` 泛放行
  - 将通用 `fs.*` 能力收敛为场景化能力接口
  - 收紧 `koma-local://` 白名单目录
  - 调整 CSP，优先去掉 `unsafe-eval`
- 依赖：
  - 建议与 `P0-3` 一起设计
- 验收标准：
  - preload 中不存在对 `controller/*` 的无差别放行
  - `koma-local://` 只能访问项目、插件数据或明确允许的资源目录
  - 生产环境 CSP 不再包含 `unsafe-eval`
  - 文件读写操作都经过主进程白名单校验

### P0-3 建立共享 IPC Contract

- 负责人角色：架构师 + 技术主管 + 前端开发 A + 后端开发 B
- 预计工期：3-4 天
- 目标：
  - 消除 preload 与 renderer 双份接口定义
  - 提升契约一致性与可维护性
- 主要改动方向：
  - 建立共享 IPC 类型层
  - 对高风险边界引入 schema 校验
  - renderer 只消费共享 contract，不自行重复定义
- 依赖：
  - 与 `P0-2` 强相关
- 验收标准：
  - preload 与 renderer 不再各自维护一套重复接口定义
  - 至少 `project`、`fs`、`chat`、`plugin` 四组接口进入共享 contract
  - 边界输入输出有最小运行时校验

### P0-4 拆壳层与核心超大文件

- 负责人角色：架构师 + 前端开发 A + 前端开发 B
- 预计工期：5-7 天
- 目标：
  - 降低 `App.tsx`、`Storyboard.tsx`、`SimpleTimeline.tsx` 的复杂度
  - 让页面退出流程编排中心
- 主要改动方向：
  - `App.tsx` 拆成 `AppShell + ProjectFlowController + GlobalOverlays`
  - `Storyboard` 拆为页面容器、任务协调层、编辑 hooks、展示组件
  - `SimpleTimeline` 拆为时间轴容器、轨道层、片段层、交互控制层
- 依赖：
  - 最好先完成 `P0-3`
- 验收标准：
  - `App.tsx` 不再直接承担主要流程编排
  - `Storyboard.tsx` 与 `SimpleTimeline.tsx` 文件体量明显下降
  - 拆分后主流程行为不变

### P0-5 补主链路 E2E 冒烟测试

- 负责人角色：测试工程师 B + 前端开发 A
- 预计工期：2-3 天
- 目标：
  - 为核心创作路径建立最低回归保护
- 建议首批用例：
  - `创建项目 -> 进入概览 -> 选择剧集 -> 进入编辑器`
  - `剧本解析 -> 分镜任务入队 -> 任务反馈可见`
- 依赖：
  - 可与 `P0-4` 并行，但最好在壳层拆分后补齐回归
- 验收标准：
  - 至少 2 条 E2E 冒烟链路进入 CI 或固定执行流程
  - 任意一次中型重构后可快速验证主链路未崩

---

## 3. P1 任务

### P1-1 明确产品北极星与目标用户

- 负责人角色：产品经理 A + 编排器
- 预计工期：1-2 天
- 目标：
  - 明确 Koma 首要是 Studio 还是 Platform
  - 明确首要服务用户
- 输出物：
  - 一页产品定义说明
  - 用户画像分层
  - 一级导航主次原则
- 验收标准：
  - 团队对“主产品目标”和“次级平台能力”达成统一口径
  - 后续任务优先级可据此判断

### P1-2 定义首次成功路径与产品级验收标准

- 负责人角色：产品经理 A + 产品经理 B + 测试工程师 A/B
- 预计工期：2 天
- 目标：
  - 建立“第一次成功”的统一衡量标准
- 输出物：
  - 首次成功路径图
  - 7-8 条产品级 Given / When / Then 验收标准
- 依赖：
  - `P1-1`
- 验收标准：
  - 项目创建、无配置、任务执行、步骤阻断、导出失败等关键场景均有明确产品行为定义

### P1-3 推荐配置与默认路径改造

- 负责人角色：产品经理 B + 前端开发 A
- 预计工期：3-4 天
- 目标：
  - 降低模型配置门槛
  - 让新用户先创作，再理解供应商
- 主要改动方向：
  - 项目概览提供“推荐配置”或“稍后配置”
  - 缺配置时给出明确阻断原因与跳转入口
- 依赖：
  - `P1-1`、`P1-2`
- 验收标准：
  - 无配置用户也能理解第一步应该做什么
  - 配置缺失时有明确说明，不再只是失败 toast

### P1-4 任务反馈下沉到上下文页面

- 负责人角色：产品经理 B + 前端开发 A
- 预计工期：3-5 天
- 目标：
  - 让用户在当前页面就能看到与当前任务直接相关的状态
- 主要改动方向：
  - 剧本页展示解析任务状态
  - 分镜页展示分镜生成/渲染状态
  - 导出页展示前置校验和执行状态
- 验收标准：
  - 关键任务不再只依赖右下角浮层
  - 失败时页面内可直接看到下一步建议

### P1-5 插件校验与权限模型统一

- 负责人角色：架构师 + 安全审计 + 后端开发 B
- 预计工期：3-5 天
- 目标：
  - 避免出现“前端允许、主进程拒绝”或反过来的规则漂移
- 主要改动方向：
  - manifest 校验统一
  - scope 执法下沉主进程
  - 安装页权限展示与运行时强制执行一致
- 验收标准：
  - 插件权限声明、安装提示和运行时限制三者一致
  - 至少 `settings:read`、`network:external`、`spawn:process` 有真实主进程约束

---

## 4. P2 任务

### P2-1 包体与依赖治理

- 负责人角色：前端开发 B + 测试工程师 B
- 预计工期：2-4 天
- 目标：
  - 降低首屏和渲染进程启动成本
- 主要改动方向：
  - 优化 `antd`、`icons`、编辑器、播放器拆包
  - 建立 chunk budget
- 验收标准：
  - 构建超大 chunk 明显收敛
  - 冷启动基线优于当前版本

### P2-2 国际化与文案统一治理

- 负责人角色：前端开发 A + 产品经理 B
- 预计工期：3-5 天
- 目标：
  - 收敛硬编码文案
  - 提升多语言一致性
- 验收标准：
  - 高频页面不再依赖大量硬编码中文
  - 新增文案遵循统一 key 规范

### P2-3 成片能力补齐

- 负责人角色：产品经理 A + 产品经理 B + 前端开发 A
- 预计工期：按迭代拆分
- 建议优先级：
  - 字幕增强
  - BGM / 基础音频控制
  - 转场
- 验收标准：
  - 至少能支撑用户完成一条“可接受成片”的基础视频

---

## 5. 推荐排期

### 第一阶段：底盘修复

- `P0-1` 密钥安全治理
- `P0-2` 收紧渲染层能力暴露
- `P0-3` 建立共享 IPC Contract
- `P0-5` 补主链路 E2E 冒烟测试

### 第二阶段：复杂度收敛

- `P0-4` 拆壳层与核心超大文件
- `P1-5` 插件校验与权限模型统一
- `P2-1` 包体与依赖治理

### 第三阶段：主路径打磨

- `P1-1` 明确产品北极星与目标用户
- `P1-2` 定义首次成功路径与验收标准
- `P1-3` 推荐配置与默认路径改造
- `P1-4` 任务反馈下沉到上下文页面

---

## 6. 团队分工建议

| 任务 | 主要负责人 | 配合角色 |
| --- | --- | --- |
| 安全与能力边界 | 架构师 / 安全审计 / 后端开发 B | 前端开发 A |
| 契约与壳层重构 | 架构师 / 技术主管 / 前端开发 A | 后端开发 B |
| 超大组件拆分 | 前端开发 A / 前端开发 B | 测试工程师 B |
| 首次成功路径 | 产品经理 A / 产品经理 B | 前端开发 A |
| E2E 与性能基线 | 测试工程师 B | 前端开发 A / B |

---

## 7. 最终执行建议

如果只能先做 5 件事，建议顺序如下：

1. `P0-1` 密钥安全治理
2. `P0-2` 收紧渲染层能力暴露
3. `P0-3` 建立共享 IPC Contract
4. `P0-5` 补主链路 E2E 冒烟测试
5. `P1-2` 定义首次成功路径与验收标准

这样做的好处是：

- 先把最危险的安全和边界问题收住
- 再补最基础的回归保护
- 最后让产品主路径有统一标准，后续所有优化都能对准一个目标
