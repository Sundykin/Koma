---

# 漫剧 AI 生成工具 (Manju Creator) 完整企划书

## 一、 产品定位

一款基于 **Electron** 开发的专业级 AI 漫剧创作工具。采用“**资产生产与后期剪辑分离**”的架构，支持多模型预设管理、项目制协作、以及基于 **DesignCombo** 的关键帧后期编排。旨在帮助创作者从 Idea 快速转化为高质量、高一致性的动态漫剧。

---

## 二、 核心模块设计

### 1. 配置中心 (Configuration Center) —— 核心引擎

* **模型预设资产化:** 模型不再是简单的配置，而是作为“资产”管理。
* **LLM 预设:** 针对剧本润色、分镜拆解配置不同的模型（如 DeepSeek, GPT-4o）。
* **视觉预设:** 支持云端 API（Nano Banana, Sora2, Kling）及 **ComfyUI 映射**（通过上传 JSON 自动映射输入节点）。
* **音效预设:** TTS 语音、背景音乐生成模型配置。


* **项目绑定:** 每个项目可绑定不同的模型组合，确保剧集风格的延续性。

### 2. 资产实验室 (Generation Labs) —— 核心生产区

此模块独立于剪辑页面，专注于“原材料”的精耕细作：

* **角色/场景库:** 利用参考图和 IP-Adapter 锁定角色特征，生成标准化的资产库。
* **剧本工作室:** 输入 Idea，自动生成剧本并拆解为 **Shot List（分镜列表）**。
* **分镜渲染间 (重中之重):** * **颗粒化控制:** 针对每一个分镜（Shot），导演可独立触发“图生成”、“视频化”、“配音生成”。
* **版本管理:** 记录每个分镜的生成历史，支持 Seed 锁定和局部重绘，只有导演确认（Confirmed）的素材才流转至剪辑端。



### 3. 编排工作台 (Editing Workspace) —— 后期总装

基于 **DesignCombo** 改造的专业非线性剪辑界面：

* **自动入轨逻辑:** 系统根据分镜列表，自动将已生成的视频、音频、字幕按序排列在时间轴上。
* **高级后期功能:**
* **图层叠加:** 在视频背景上叠加透明角色立绘。
* **关键帧系统:** 支持对素材的缩放、位移、透明度进行关键帧动画处理。
* **素材扩展:** 支持添加额外的贴纸、转场、环境音效、特效 BGM。


* **实时预览:** 利用浏览器引擎与 Remotion 逻辑，提供接近成品效果的流畅预览。

---

## 三、 技术架构与流程

### 1. 技术栈

* **框架:** Electron + React + Tailwind CSS。
* **剪辑引擎:** DesignCombo (UI 交互) + Remotion (渲染描述)。
* **处理引擎:** 本地 FFmpeg (负责分片渲染、转码、无损合并)。
* **通信协议:** 基于 JSON 的 **Manju-DSL**，贯穿资产生成与剪辑编排。

### 2. Manju-DSL 核心协议 (简化版)

```json
{
  "projectId": "p101",
  "shots": [
    {
      "shotId": "s01",
      "content": { "image": "path/shot01.png", "video": "path/shot01.mp4", "audio": "path/voice01.mp3" },
      "timing": { "start": 0, "duration": 5.0 }
    }
  ],
  "timeline": {
    "layers": [
      { "type": "main_shot", "clips": [ ... ] },
      { "type": "overlay_char", "clips": [ { "source": "susu_pose1.png", "keyframes": [...] } ] }
    ]
  }
}

```

---

## 四、 核心竞争优势

1. **漏斗式工作流:** 将最耗时的 AI 生成工作（实验室模块）与最细致的编排工作（剪辑页面）分离。导演在生成阶段关注“质感”，在剪辑阶段关注“节奏”。
2. **全模型适配:** 通过“预设”机制屏蔽底层技术差异。无论是自建 ComfyUI 还是第三方大模型（Sora2 等），在工具层面上操作逻辑统一。
3. **高性能导出:** 利用分片渲染策略，配合本地硬件加速 FFmpeg，解决 Electron 端处理长视频的性能瓶颈。
4. **自由干预:** 导演模式允许在任何环节介入。不仅能换模型、换提示词，还能在最后阶段手动调整关键帧，克服纯 AI 生成的随机性。

---

## 五、 开发路线图 (Roadmap)

1. **第一阶段 (基础设施):** 开发 Electron 模型预设管理系统，支持基础 API 调用和项目制目录结构。
2. **第二阶段 (实验室流程):** 实现“剧本 -> 分镜列表”的自动化拆解，并完成单个分镜的“图-音-视”生成闭环。
3. **第三阶段 (剪辑总装):** 集成 DesignCombo，实现分镜素材的自动入轨，并开发基于 Remotion 的关键帧动画逻辑。
4. **第四阶段 (性能与导出):** 优化 FFmpeg 本地渲染管线，支持一键导出 4K 漫剧，或导出工程文件至剪映/PR。

---

## 六、 总结

本工具不只是为了“生成视频”，而是为了建立一套**可控的、工业化的漫剧生产标准**。通过将 AI 的灵活性与传统剪辑的精确性结合，让个人创作者也能制作出具备商业水准的漫剧作品。

---

**下一步建议：**
这份企划书可以作为您开发或融资的技术白皮书。**您希望我接下来为您编写“分镜渲染间”的 UI 组件原型代码，还是为您详细制定“从分镜列表到 DesignCombo 时间轴”的数据映射函数？**