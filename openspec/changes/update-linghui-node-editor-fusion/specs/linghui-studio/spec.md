## ADDED Requirements

### Requirement: Node-Centric Split Editor Overlay

灵绘 SHALL 在轻编辑态下围绕当前节点渲染双区编辑浮层，由上方紧凑工具条和下方主编辑面板组成，并保持节点主体在编辑过程中可见。

#### Scenario: 打开节点轻编辑态
- **WHEN** 用户点击画布上的图片节点、视频节点或其他支持轻编辑态的节点
- **THEN** 系统在节点上方显示紧凑工具条
- **AND** 在节点下方显示主编辑面板
- **AND** 当前节点本体保持可见，不被主编辑面板直接覆盖

#### Scenario: 节点靠近视口边缘
- **WHEN** 当前节点位于画布边缘，默认上下双区布局无法完整显示
- **THEN** 系统对工具条或主编辑面板执行左右偏移或侧向避让
- **AND** 仍然保持当前节点主体主要区域可见
- **AND** 不因为避让而退回覆盖节点主体的单块大卡片

### Requirement: Mode-Adaptive Image Node Editing

系统 SHALL 根据图片节点当前的 `generate` 或 `import` 模式裁剪展示内容，而不是始终渲染同一套编辑控件。

#### Scenario: 图片节点处于生成模式
- **WHEN** 用户打开处于 `generate` 模式的图片节点
- **THEN** 系统展示上游参考摘要、提示词编辑器和生成参数
- **AND** 将上传图片能力收敛为紧凑附加入口，而不是默认展示大面积上传区
- **AND** 优先突出该节点从上游输入生成图片的主路径

#### Scenario: 图片节点处于导入输出模式
- **WHEN** 用户打开处于 `import` 模式的图片节点
- **THEN** 系统展示图片预览、上传、替换和清空操作
- **AND** 隐藏提示词编辑器、生图渠道、宫格、批量和其他仅生成模式有意义的参数
- **AND** 明确该节点当前用于向下游提供图片产物

### Requirement: Mode-Adaptive Video Node Editing

系统 SHALL 根据视频节点当前的“生成”或“导入”状态裁剪展示内容，而不是把上传、提示词和生成参数全部堆叠在同一个面板里。

#### Scenario: 视频节点处于生成状态
- **WHEN** 用户打开未挂载本地视频素材的视频节点
- **THEN** 系统展示图片、视频、音频等上游输入摘要
- **AND** 展示提示词编辑器、参考模式和视频生成参数
- **AND** 不再将大型上传区作为主编辑区的默认核心内容

#### Scenario: 视频节点处于导入状态
- **WHEN** 用户打开已挂载本地视频素材的视频节点
- **THEN** 系统展示视频预览、上传、替换和清空操作
- **AND** 隐藏提示词编辑器、参考模式和仅生成模式有意义的参数
- **AND** 明确该节点当前用于输出既有视频产物给下游复用

### Requirement: Detached Image and Video Tool Surfaces

系统 SHALL 将图片和视频节点的增强工具能力设计为独立工具入口和次级工具面板，而不是作为主编辑区中的默认大块内容。

#### Scenario: 打开图片或视频工具
- **WHEN** 用户在节点轻编辑态下点击多角度、扩图、打光、重绘、高清、解析或合成等工具入口
- **THEN** 系统打开独立于主编辑区的次级工具面板
- **AND** 主编辑区的核心结构保持稳定
- **AND** 关闭工具后，节点回到原有主编辑状态

#### Scenario: 导入模式下访问生成型工具
- **WHEN** 用户在图片或视频节点的导入模式下触发仅生成模式有效的工具
- **THEN** 系统明确提示该工具依赖生成模式
- **AND** 不把该工具的整块配置直接塞入当前导入表单

### Requirement: Integrated Prompt Editing Surface

系统 SHALL 让节点弹窗中的提示词编辑器与外层浮层共享统一的视觉层级，并保留现有 `@` 引用能力。

#### Scenario: 在生成模式中编辑提示词
- **WHEN** 用户在图片节点或视频节点的生成模式中打开提示词区域
- **THEN** 提示词编辑器与节点弹窗共享同一背景层次和圆角体系
- **AND** 不再呈现明显的“卡片中再套卡片”割裂感
- **AND** `@` 引用补全和引用预览能力继续可用

#### Scenario: 导入模式下打开节点弹窗
- **WHEN** 用户打开图片节点或视频节点的导入模式
- **THEN** 系统不渲染当前模式无效的提示词编辑区
- **AND** 节点弹窗整体视觉密度保持紧凑
