## 1. Execution Planning

- [x] 1.1 将灵绘执行计划从线性拓扑序升级为按层分组的拓扑层结构
- [x] 1.2 扩展执行队列状态以表达多个运行中节点，并保持现有调用方兼容

## 2. Parallel Scheduling

- [x] 2.1 改造 `executeLinghuiWorkflow` 为逐层推进、层内并发执行
- [x] 2.2 保持取消与失败隔离语义，确保失败分支不会误取消同层其他节点

## 3. Validation

- [x] 3.1 补充工作流测试，覆盖并发启动、依赖等待和队列状态
- [x] 3.2 运行目标 `vitest` 用例确认并行调度实现通过
