## 1. 工作区基础架构

- [x] 1.1 创建 `StoryboardWorkspace` 组件，作为分镜工作区的主容器（顶部工具栏 + 分镜列表区域）
- [x] 1.2 修改 `EditorView.tsx`，移除三步导航逻辑，直接渲染 `StoryboardWorkspace`
- [x] 1.3 修改 `App.tsx` 中 `view='editor'` 的路由逻辑，移除 `editorStep` 状态机
- [x] 1.4 简化 `EpisodeStepProgress` 类型，移除 assets/storyboard/video 步骤进度
- [x] 1.5 添加空 Episode 引导状态（提示导入剧本或手动添加分镜）

## 2. 分镜卡片布局重构

- [x] 2.1 重构 `ShotCard.tsx` 为左右两区布局：左侧图片/视频预览区（45%）+ 右侧编辑区（55%）
- [x] 2.2 实现图片预览区：大图显示（最小高度 280px）+ 底部候选缩略图列表 + 点击放大
- [x] 2.3 实现视频预览区：视频播放器 + 候选视频切换 + 播放控制
- [x] 2.4 实现参考图列表区域：显示角色/场景参考图小图
- [x] 2.5 重构提示词输入区：多行文本框（最小高度 120px）+ Tab 切换图片/视频提示词
- [x] 2.6 实现文案编辑区：多行编辑 + 角色匹配标签显示
- [x] 2.7 实现卡片顶部操作栏（序号、生成按钮、批量数量、视频口型、风格标签）
- [x] 2.8 实现卡片底部操作栏（编辑、推理、评论、结构、覆盖分镜、删除分镜）
- [x] 2.9 更新 `storyboardConstants.ts` 中的布局常量
- [x] 2.10 实现响应式折叠（<1024px 时上下堆叠）

## 3. 工具面板框架

- [x] 3.1 创建 `ToolPanelDrawer` 通用组件（Ant Design Drawer 封装，宽度 35-45%，最小 400px）
- [x] 3.2 创建工具面板状态管理（当前打开的面板 ID、面板临时状态缓存）
- [x] 3.3 实现工具栏按钮组（剧本工作室、资产管理、章节推理、风格设置、导出、AI 助手）
- [x] 3.4 实现面板切换逻辑（切换时保留旧面板临时状态）

## 4. 剧本工作室面板

- [x] 4.1 创建 `ScriptStudioPanel` 组件，包含文本粘贴区域和文件导入按钮
- [x] 4.2 实现文件导入（.txt、.md、.srt）和面板内文本预览
- [x] 4.3 实现渐进式处理流程 UI（步骤指引：导入 → 精炼 → 章节划分 → 拆分分镜 → 确认）
- [x] 4.4 实现 AI 分镜拆分功能：调用 LLM → 面板内预览 → 手动调整（合并/拆分/编辑）
- [x] 4.5 实现章节划分功能（智能划分 + 均匀划分两种模式）
- [x] 4.6 实现确认导入：将拆分结果写入当前 Episode 并刷新分镜列表
- [x] 4.7 集成内容精炼功能（浓缩/扩写/润色模板调用）

## 5. 资产管理面板

- [x] 5.1 创建 `AssetManagerPanel` 组件，包含角色/场景/道具三个 Tab
- [x] 5.2 实现资产卡片列表（名称 + 缩略图 + 描述摘要）
- [x] 5.3 实现"从剧本提取"功能（调用 LLM 分析分镜文案，提取角色/场景/道具）
- [x] 5.4 实现资产参考图生成（调用 TTI 服务）
- [x] 5.5 实现资产详细编辑视图（名称、描述、别名、参考图上传/替换）

## 6. 章节推理面板

- [x] 6.1 创建 `ChapterInferencePanel` 组件，显示当前章节分镜概览
- [x] 6.2 实现批量推理提示词功能（调用 LLM 为章节所有分镜生成提示词 + 进度显示）
- [x] 6.3 实现推理模板选择（基础/进阶/工作室级模板列表 + 模板预览）
- [x] 6.4 实现批量改写文案功能（AI 改写 + 前后对比 + 逐条确认/全部接受）

## 7. 风格设置面板

- [x] 7.1 创建 `StyleSettingsPanel` 组件，以卡片网格展示全局风格库
- [x] 7.2 实现风格搜索和分类筛选功能
- [x] 7.3 实现一键应用风格到当前项目（更新 styleSnapshot）
- [x] 7.4 实现面板内新建自定义风格表单
- [x] 7.5 实现切换风格后提示用户是否重新推理提示词

## 8. 导出中心面板与分镜直出

- [x] 8.1 创建 `ExportCenterPanel` 组件，展示所有导出选项和说明
- [x] 8.2 创建 `StoryboardExportService`，从 projectStore shots 数据直接构建导出数据
- [x] 8.3 实现快速视频导出（分镜顺序拼接，参数配置：分辨率/静帧时长/音频/字幕）
- [x] 8.4 实现分镜直出剪映草稿导出（复用 JianyingExporter 模板，Shot→剪映片段映射）
- [x] 8.5 实现图片序列导出（按序号导出，支持超分辨率/格式选择/范围选择）
- [x] 8.6 实现导出进度显示和导出历史记录
- [x] 8.7 实现"高级编辑"入口（跳转到 SimpleEditor，自动加载分镜到时间线）

## 9. 提示词模板扩展

- [x] 9.1 扩展 `PromptTemplateType` 枚举，新增剧本转换/内容精炼/章节划分/爆款开头/批量改写模板类型
- [x] 9.2 在 `promptTemplates.ts` 中注册基础/进阶/工作室级剧本转换模板
- [x] 9.3 注册内容精炼模板（浓缩、扩写、润色）
- [x] 9.4 注册章节划分模板（智能划分、均匀划分）
- [x] 9.5 注册爆款开头模板和批量改写模板
- [x] 9.6 在各工具面板的子流程中引用对应模板

## 10. 分镜列表增强

- [x] 10.1 实现分镜批量选择（Checkbox + Shift+Click 多选）
- [x] 10.2 实现批量操作工具栏（批量生成图片/视频、批量删除、批量推理）
- [x] 10.3 实现章节筛选功能（章节标签过滤分镜列表）
- [x] 10.4 将工具栏中的章节切换下拉和搜索/筛选功能集成
