## 1. Video Editor Simplification

- [x] 1.1 重构 `VideoNodeEditor.tsx` 与 `VideoNodeEditorPanels.tsx`，删除视频编辑框中的结果预览区、模式切换区和常驻说明文案
- [x] 1.2 为视频能力、输入摘要、工具入口和关键参数补齐 Ant Design `Tooltip`，只保留简洁标签与控件
- [x] 1.3 将视频比例、分辨率、时长调整为三个独立选择器，并保持生成态下的稳定布局

## 2. Pass-Through Video Nodes

- [x] 2.1 调整画布导入视频路径（含上传到画布与拖拽到画布），让导入视频直接创建透传视频节点
- [x] 2.2 移除视频编辑框中的“导入输出”显式切换，改为依据节点来源隐式判定生成节点与透传节点
- [x] 2.3 确保透传视频节点不渲染提示词、能力切换、工具预设和生成参数，并可直接连接给下游节点

## 3. Inline Playback On Video Nodes

- [x] 3.1 改造 `nodes/VideoNode.tsx`，让已生成的视频结果直接在节点卡片内联预览与播放
- [x] 3.2 为节点播放按钮添加独立事件处理，确保点击播放或暂停时不会弹出视频编辑框
- [x] 3.3 删除与编辑框视频预览相关的冗余样式和交互，只保留节点本身作为结果播放载体

## 4. Validation

- [x] 4.1 补充或更新视频节点编辑器、透传节点与节点播放交互测试
- [x] 4.2 验证视频节点在文生视频、图生视频、参考生视频、首尾帧视频下仍按模型能力正确显示参数与输入
- [x] 4.3 验证本次实现不引入任何旧数据兼容逻辑，并通过 `tsc` 与 `vitest`
