## 1. Overlay Layout

- [x] 1.1 重构 `LinghuiNodeEditor` 轻编辑态为“上方工具条 + 节点可视空隙 + 下方主编辑面板”
- [x] 1.2 为轻编辑态补齐节点安全区域与视口边缘避让策略，确保面板不覆盖节点主体
- [x] 1.3 保留折叠态和沉浸式态切换，并让新布局与现有节点选中逻辑兼容

## 2. Image / Video Editor Restructure

- [x] 2.1 重构 `ImageNodeEditor`，让 `generate` 模式聚焦上游摘要、提示词和生成参数
- [x] 2.2 重构 `ImageNodeEditor`，让 `import` 模式只展示素材预览和导入操作，隐藏无关提示词与生成参数
- [x] 2.3 重构 `VideoNodeEditor`，让生成模式聚焦多模态输入摘要、提示词和视频参数
- [x] 2.4 重构 `VideoNodeEditor`，让导入模式只展示视频预览和导入操作，隐藏无关生成控件

## 3. Tool Surface Separation

- [x] 3.1 将图片节点的多角度、扩图、打光、重绘等能力拆为独立工具入口和次级工具面板
- [x] 3.2 将视频节点的高清、解析、合成等能力拆为独立工具入口和次级工具面板
- [x] 3.3 明确导入模式下的工具可用性和切换提示，避免工具状态与主模式冲突

## 4. Prompt Editor Visual Integration

- [x] 4.1 调整 `LinghuiPromptEditor` 在节点弹窗中的背景、边框和层级样式，使其与外层面板视觉融合
- [x] 4.2 保留 `@` 引用补全、预览和键盘交互，不因视觉改造影响已有能力

## 5. Validation

- [x] 5.1 验证图片和视频节点在不同模式下不会出现无效控件
- [ ] 5.2 验证轻编辑态在常见视口和缩放比例下不会遮挡当前节点
- [x] 5.3 验证工具面板开合不会导致主编辑区大幅跳动或节点失焦
