# Koma 多 Agent 团队分析报告

## 1. 分析说明

本轮报告在上一版人工分析基础上，按 `agent-prompts` 继续组织了一轮多角色会诊，目标不是重复结论，而是把问题收敛成更适合团队执行的版本。

本次纳入的角色：

| 角色 | 对应提示词 | 本轮关注点 |
| --- | --- | --- |
| 编排器 | `agent-prompts/00-orchestrator.md` | 路由、验证门、优先级仲裁 |
| 架构师 | `agent-prompts/01-architect.md` | 系统边界、壳层、插件、IPC、存储演进 |
| 产品经理 A | `agent-prompts/02-product-manager-requirement.md` | 产品定义、目标用户、优先级、验收标准 |
| 产品经理 B | `agent-prompts/03-product-manager-ux.md` | 用户旅程、空状态、错误恢复、可访问性 |
| 技术批评家 | `agent-prompts/13-technical-critic.md` | 复杂度、维护性、容错性、过度设计 |
| 安全审计 | `agent-prompts/11-security-auditor.md` | Electron / IPC / 插件 / 本地文件能力 |
| 测试工程师 B | `agent-prompts/10-tester-performance.md` | 性能风险、E2E 空洞、验证方案 |

本轮还做了两项现状验证：

- `npm --prefix frontend test -- --run`：通过，49 个测试全部通过
- `npm --prefix frontend run build`：通过，但存在多个超大 chunk 警告

---

## 2. 项目现状快照

### 2.1 总体判断

Koma 已经不是 Demo，而是一个具备完整创作链路雏形的桌面产品：

- 从项目创建、剧本编辑、资产分析、分镜生成，到视频编辑、导出，主路径基本存在
- Electron 主进程、React 前端、插件 SDK、Chat/MCP/Agent、Provider 体系已经具备平台化倾向
- 当前最大问题不是“有没有功能”，而是“边界是否清晰、复杂度是否可控、主路径是否足够明确”

### 2.2 客观信号

| 维度 | 现状 |
| --- | --- |
| 前端组件体量 | `frontend/src/components` 约 27182 行 |
| 前端服务体量 | `frontend/src/services` 约 8723 行 |
| 前端状态体量 | `frontend/src/store` 约 7420 行 |
| 聊天模块体量 | `frontend/src/chat` 约 3836 行 |
| Electron 服务体量 | `electron/service` 约 6921 行 |
| 超大文件 | `Storyboard.tsx` 1337 行，`SimpleTimeline.tsx` 1100 行，`trackStore.ts` 923 行 |
| 自动化测试 | 仅 3 个测试文件，49 个测试 |
| 构建表现 | 前端构建通过，但 `vendor-antd`、`vendor-antd-icons`、`vendor-ui` 均过大 |
| 类型安全 | `frontend/tsconfig.json` 未开启 `strict` |
| 国际化 | 历史审计记录 `frontend/src/components` 中存在 2200+ 硬编码中文 |

### 2.3 核心结论

- 工程规模已经超过“靠记忆维护”的阶段
- 当前测试密度、边界治理和安全模型不足以支撑继续快速堆功能
- 产品主流程可走通，但首次成功路径仍然偏长，配置门槛也偏高

---

## 3. 架构师视角

### 3.1 优点

- 技术方向是对的：Electron + React + Plugin SDK + Chat/MCP/Agent + Provider 扩展，具备长期产品延展性
- 代码里能看到从单体走向模块化的迹象，例如 `projectStore.ts` 与 `globalStore.ts` 已经开始做聚合导出
- 项目目录化存储对本地桌面工具当前阶段是务实方案，便于恢复和人工排障

### 3.2 关键问题

#### 问题 1：应用壳层过重

`frontend/src/App.tsx` 同时处理页面切换、项目/剧集状态、步骤状态、任务监听、分析数据加载、模态开关，已经在承担流程编排职责，而不仅是路由和挂载。

证据：

- `frontend/src/App.tsx:54-220`
- `frontend/src/App.tsx:266-344`

影响：

- 任意主流程变更都会回流到壳层
- 页面级状态和领域状态耦合，难以独立测试
- 未来加多窗口、工作区恢复、并发项目会快速失控

#### 问题 2：IPC 不是契约优先，而是通道优先

`preload` 和 renderer 两侧重复定义大量接口；`controller/*` 在 `preload` 校验里被直接放行，接口暴露面过宽。

证据：

- `electron/preload/bridge.ts:24-39`
- `electron/preload/bridge.ts:69-223`
- `frontend/src/services/electronService.ts:27-123`

影响：

- 契约没有唯一事实来源
- 新增主进程能力时，极易自然扩张成渲染层能力
- 安全审计和版本演进成本上升

#### 问题 3：插件平台扩展性快于隔离能力

前端插件通过 `koma-local://` + script 注入 + `window.__KOMA_PLUGIN_*` 全局导出加载；后端插件从用户目录动态 `require`，并可衍生进程能力。

证据：

- `frontend/src/services/plugin/PluginLoader.ts:229-260`
- `electron/service/plugin/runtime.ts:90-111`
- `electron/service/plugin/runtime.ts:420-447`

影响：

- 插件系统越成功，运行时风险越高
- 平台目标与当前安全模型不匹配

#### 问题 4：存储写路径分散，没有明确一致性单元

页面、工具栏、剧集管理、自动保存等多个 UI 模块都能直接触发持久化，而底层仍是多 JSON 文件，无事务和统一工作单元。

证据：

- `frontend/src/components/project/ProjectOverview.tsx:136-186`
- `frontend/src/components/project/ScriptWorkbench.tsx:51-133`
- `electron/service/project.ts:186-240`

影响：

- 容易出现时序性问题和难复现 bug
- 项目规模变大后，一致性风险会持续上升

### 3.3 架构建议

1. 建共享 IPC Contract 层，合并 preload 与 renderer 的重复接口声明。
2. 拆 `AppShell + ProjectFlowController + GlobalOverlays`，让壳层退出流程编排。
3. 将插件校验、权限和激活规则收敛到主进程或共享契约层。
4. 为高频写路径建立统一写模型，逐步从“页面自管写入”过渡到“领域 service 管写入”。

---

## 4. 前端负责人视角

### 4.1 优点

- UI 风格统一，创作工具气质明确
- 重型页面已做懒加载，说明团队有包体治理意识
- 任务状态栏、自动保存、错误边界、i18n 基础设施都已存在，说明不是纯功能堆砌

### 4.2 关键问题

#### 问题 1：重型组件太多

核心区出现多个 700 行以上文件，且集中在真正的生产力区域：

- `Storyboard.tsx` 1337 行
- `SimpleTimeline.tsx` 1100 行
- `trackStore.ts` 923 行
- `useStoryboardHandlers.ts` 874 行
- `SimplePropertiesPanel.tsx` 758 行

影响：

- 改一个需求容易牵动 UI、状态和持久化三层
- reviewer 难以快速识别回归风险

#### 问题 2：类型系统没有真正接管质量

前端未开 `strict`，IPC、插件、workflow、Provider 边界存在大量 `any`。

证据：

- `frontend/tsconfig.json:2-21`
- `electron/preload/bridge.ts:77-223`
- `frontend/src/services/electronService.ts:58-68`

影响：

- 重构保护不足
- “字段存在但语义错了”的问题更容易漏掉

#### 问题 3：保存状态与任务状态割裂

`ScriptWorkbench` 维护自己的保存节奏，`TaskStatusBar` 又维护全局任务浮层，用户要自己拼接“本地保存”和“AI 后台任务”两套反馈。

证据：

- `frontend/src/components/project/ScriptWorkbench.tsx:51-133`
- `frontend/src/components/common/TaskStatusBar.tsx:193-260`

#### 问题 4：包体已经进入高压区

本次构建产物中：

- `vendor-antd` 约 1.4MB
- `vendor-antd-icons` 约 995KB
- `vendor-ui` 约 960KB

证据：

- `frontend/vite.config.ts:14-65`
- 本次 `npm --prefix frontend run build` 输出

影响：

- 首屏、弱机器、Electron 渲染进程启动成本都会被持续拉高

### 4.3 前端建议

1. 先拆 `Storyboard`、`SimpleTimeline`、`trackStore` 三个热点。
2. 先从 IPC、插件、Provider 配置边界引入 `unknown + schema`，不要一次性全局 strict。
3. 统一保存状态、任务状态、步骤状态的反馈模型。
4. 对 `antd`、`icons`、编辑器、播放器做更细粒度拆包。

---

## 5. 产品经理视角

### 5.1 关键判断

Koma 最大的产品优势是“创作闭环雏形”，但最大的问题也是“主路径还不够单一”。

当前系统同时像：

- AI 短剧创作工具
- 通用 AI 聊天工作台
- 插件 / MCP / Agent 平台容器

如果不先统一北极星，会持续拉扯 P0 优先级。

### 5.2 关键问题

#### 问题 1：产品目标不够聚焦

证据：

- `README.md`
- `frontend/src/components/common/Sidebar.tsx:68-176`

后果：

- 功能优先级摇摆
- 一级导航被主链路外的入口稀释

#### 问题 2：目标用户没有被明确分层

当前至少混入了三类用户：

- 创作者
- 新手视频用户
- 插件 / AI 高级用户

后果：

- 默认配置、引导深度、信息架构难以优化

#### 问题 3：缺少“首次成功”标准

当前没有明确回答：

- 新用户多快能进入可编辑剧本区
- 没有模型配置时怎样跑通默认路径
- 任务失败后系统如何引导恢复

### 5.3 建议补充的验收标准

1. Given 首次打开应用且没有历史项目，When 用户进入首页，Then 用户应在 3 步内进入可编辑的剧本工作区。
2. Given 用户没有任何模型配置，When 创建项目后进入项目概览，Then 系统应提供默认推荐方案或明确下一步配置引导。
3. Given 用户未选择剧集或剧本为空，When 用户尝试进入后续制作步骤，Then 系统必须明确说明阻断原因和推荐动作。
4. Given 用户发起剧本解析或分镜生成，When 任务持续超过 5 秒，Then 当前页面必须展示任务状态、阶段信息和完成反馈。
5. Given 用户缺少必要配置，When 点击依赖该配置的功能，Then 系统必须说明缺失项、影响范围和跳转修复入口。
6. Given 用户编辑剧本后切换页面或剧集，When 自动保存完成，Then 用户必须看到保存成功状态，且重新进入后内容一致。
7. Given 用户尝试导出视频，When 项目缺少必要素材、时间线或配置，Then 系统必须先做前置校验并列出缺失项，而不是直接报错。
8. Given 用户首次导出失败，When 系统返回失败结果，Then 错误信息必须包含问题摘要、可能原因和下一步修复建议。

---

## 6. UX 视角

### 6.1 关键用户旅程

当前主路径大致是：

`打开应用 -> 创建项目 -> 理解配置区 -> 选择/创建剧集 -> 编写剧本 -> 触发解析 -> 资产 -> 分镜 -> 剪辑 -> 导出`

对熟手可接受，对新手明显偏长。

### 6.2 高价值 UX 问题

1. 首次打开只有“创建第一个项目”，没有“你将做成什么”的预期。
   证据：`frontend/src/components/project/ProjectList.tsx:126-139`

2. 项目概览页过早暴露 LLM / TTI / ITV / TTS 四组配置，认知负担高。
   证据：`frontend/src/components/project/ProjectOverview.tsx:223-289`

3. 任务反馈主要依赖右下角浮层，与当前页面上下文绑定不够强。
   证据：`frontend/src/components/common/TaskStatusBar.tsx:193-260`

4. 步骤切换存在，但“为什么能去下一步 / 为什么不能去下一步”解释不够明确。
   证据：`frontend/src/components/editor/EditorView.tsx:44-145`

5. 空状态多数还是占位，不够指导。
   证据：`frontend/src/components/project/ProjectList.tsx:126-139`、`frontend/src/components/project/ScriptWorkbench.tsx:226-239`

6. 可访问性和键盘可达性仍有缺口。
   证据：`frontend/src/components/common/Sidebar.tsx:28-50`、`frontend/src/components/common/StepNavigator.tsx`

7. 固定桌面布局明显，小屏、分屏和窄窗口策略没有被明确设计。
   证据：`frontend/src/components/project/ProjectOverview.tsx` 中固定左右栏宽度 + 全局侧边栏组合

### 6.3 UX 建议

1. 为空项目页增加示例项目、推荐起点和 3 步上手说明。
2. 提供推荐配置或“稍后配置”，不要让供应商决策挡在创作前。
3. 把任务反馈下沉到剧本、分镜、渲染页面上下文。
4. 把步骤锁定原因改成明确缺口提示，而不是通用文案。
5. 为步骤导航、侧栏、折叠按钮补齐键盘与屏幕阅读器语义。

---

## 7. 安全审计视角

### 风险 1：明文保存 API Key 和模型配置

- 严重度：S1
- 证据文件：
  - `frontend/src/store/encryption.ts`
  - `frontend/src/store/settings/core.ts`
- 风险说明：
  - `encryptSettings` / `decryptSettings` 实际不做加密
  - 设置可直接写入 `settings.json` 或 localStorage
- 修复建议：
  - 接入系统安全存储
  - 普通设置与密钥分离
  - 由主进程代理持有长期密钥

### 风险 2：渲染进程暴露通用文件系统能力

- 严重度：S2
- 证据文件：
  - `electron/preload/bridge.ts`
  - `electron/controller/fs.ts`
- 风险说明：
  - `readFile`、`writeFile`、`remove`、`copy`、`mkdir`、`readdir`、`stat` 等能力被直接暴露给渲染层
  - 一旦 renderer 被劫持，攻击面可直达本地文件系统
- 修复建议：
  - 收敛为能力型 API
  - 在主进程做目录归属校验
  - 对高风险操作增加审计和确认

### 风险 3：CSP 过宽

- 严重度：S2
- 证据文件：
  - `electron/service/security.ts`
- 风险说明：
  - 允许 `unsafe-inline` 与 `unsafe-eval`
  - `connect-src`、`img-src`、`media-src` 也较宽
- 修复建议：
  - 优先移除 `unsafe-eval`
  - 生产环境改为最小权限 CSP

### 风险 4：插件可通过 `settings:read` 间接读取嵌套密钥

- 严重度：S2
- 证据文件：
  - `frontend/src/services/plugin/PluginAPI.ts`
- 风险说明：
  - 顶层字段不敏感，但内部数组对象可能包含 `apiKey`
- 修复建议：
  - 深层脱敏
  - 将密钥字段从普通配置中拆出

### 风险 5：`koma-local://` 协议允许读取过宽范围的本地文件

- 严重度：S2
- 证据文件：
  - `electron/service/protocol.ts`
- 风险说明：
  - 默认允许 `home`、`appData`、`userData`、`temp` 下任意文件
- 修复建议：
  - 收敛到项目目录、插件数据目录和资源白名单

---

## 8. 性能与 E2E 视角

### 风险 1：包体过大

证据：

- 本次 `npm --prefix frontend run build`
- `frontend/vite.config.ts:14-65`

表现：

- `vendor-antd` 约 1.4MB
- `vendor-antd-icons` 约 995KB
- `vendor-ui` 约 960KB

### 风险 2：分镜页是重型单体组件

证据：

- `frontend/src/components/storyboard/Storyboard.tsx:87-240`

影响：

- 大镜头数时容易出现渲染卡顿和任务完成后的大范围重渲染

### 风险 3：时间线渲染复杂度高

证据：

- `frontend/src/components/editor/SimpleTimeline.tsx:1-260`

影响：

- 长素材、缩放、拖拽和关键帧场景下容易掉帧

### 风险 4：状态仓库过大

证据：

- `frontend/src/store/trackStore.ts`

影响：

- 单次交互可能触发大范围状态扩散

### 风险 5：主流程 E2E 回归保护几乎空白

证据：

- `frontend/src/components/common/ErrorBoundary.test.tsx`
- `frontend/src/utils/errorHandler.test.ts`
- `frontend/src/utils/urlUtils.test.ts`

影响：

- 主流程回归主要靠人工

### 最小验证方案

1. 记录冷启动到首个可交互页面时间，建立现状基线。
2. 做一个 200 shot 的 Storyboard 数据集，测试首次渲染、切换 active shot、单任务完成后的重渲染范围。
3. 做一个 20 轨、每轨 15 个 clip 的 Timeline 场景，在不同缩放下测 FPS。
4. 增加两条最小 E2E 冒烟：
   - `创建项目 -> 进入概览 -> 选择剧集 -> 进入编辑器`
   - `打开现有项目 -> 进入分镜/时间线 -> 完成一次关键交互`

---

## 9. 多 Agent 共识

多角色会诊后，最重要的共识不是某一个页面或功能，而是以下四点：

1. **当前最大的风险不是功能缺口，而是边界失控。**
   体现在 preload、FS、插件、CSP、本地协议、重复契约和壳层编排上。

2. **当前最大的产品问题不是没有流程，而是主流程不够单一。**
   创作工具和 AI/插件平台两条定位同时外露，稀释了首次成功路径。

3. **当前最大的工程问题不是没有测试，而是测试密度与系统复杂度严重不匹配。**
   在超大组件和宽边界背景下，这会快速放大重构风险。

4. **现在最该优先做的是收权限边界、收主路径定义、收工程复杂度。**
   而不是继续优先横向扩功能。

---

## 10. 优先级行动清单

### P0

1. 修复 API Key 明文存储，接入系统安全存储。
2. 收紧渲染层攻击面：CSP、`koma-local://` 白名单、通用 FS API 暴露面。
3. 统一 IPC / preload / renderer 契约。
4. 拆 `App.tsx` 和至少两个核心超大文件。
5. 为主链路补最少 1 到 2 条 E2E 冒烟用例。

### P1

1. 明确产品北极星：Studio 主链路与 Chat / 插件平台的主次关系。
2. 增加首次成功路径的产品级验收标准。
3. 为项目概览提供默认推荐配置和更强引导。
4. 将任务反馈从全局浮层下沉到上下文页面。
5. 统一保存状态、任务状态、步骤状态的用户反馈。

### P2

1. 包体治理与性能基线建设。
2. i18n 治理和文案统一。
3. 字幕、BGM、转场、音频增强等成片能力补齐。

---

## 11. 最终结论

如果用一句话总结当前项目：

**Koma 已经具备成为“AI 视频创作工作台”的骨架，但现在最需要的不是继续横向扩功能，而是把安全边界、架构契约、前端复杂度和产品主路径一起收紧。**

站在团队视角，当前最值得优先做的不是“再加一个模块”，而是：

- 把权限边界收回来
- 把主路径定义讲清楚
- 把超大组件拆开
- 把主流程回归保护补上

做到这一步，后续再扩插件、Chat、Agent、Provider 和高级视频能力，成本和成功率都会高很多。
